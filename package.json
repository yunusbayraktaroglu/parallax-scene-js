{
	"name": "parallax-scene-js",
	"description": "Create raw WebGL parallax scenes with only 1 draw call.",
	"author": "Yunus Bayraktaroglu",
	"private": true,
	"license": "MIT",
	"scripts": {
		"build": "turbo build",
		"build:packages": "turbo build --filter=./packages/*",
		"build:demos": "turbo build --filter=@pronotron/demos",
		"build:docs": "turbo docs --filter=@pronotron/docs",
		"dev": "turbo dev",
		"test": "turbo test --filter=./packages/*",
		"test:watch": "turbo test:watch --filter=./packages/*",
		"lint": "turbo lint --filter=./packages/*",
		"clean": "turbo clean && rm -rf node_modules",
		"format": "prettier --write \"**/*.{ts,tsx,md}\"",
		"changeset": "changeset",
		"version-packages": "changeset version",
		"release": "turbo build --filter=./packages/* && changeset publish"
	},
	"devDependencies": {
		"@changesets/cli": "^2.29.7",
		"eslint": "^9.38.0",
		"prettier": "^3.6.2",
		"turbo": "^2.5.8"
	},
	"engines": {
		"node": ">=18"
	},
	"packageManager": "npm@10.2.4",
	"workspaces": [
		"apps/*",
		"packages/*"
	]
}