import { ParallaxManager } from "../src/";

describe( "Parallax Scene JS (unit)", () => {

	let PARALLAX_MANAGER: ParallaxManager;

	beforeEach( () => {
		//PARALLAX_MANAGER = new ParallaxManager();
	} );

	test( "tests running", () => {

		expect( 1 ).toBe( 1 );

	} );

} );
